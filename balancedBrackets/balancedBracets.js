// Given a string, return true if it contains all balanced parenthesis (),
// curly-brackets {}, and square-brackets [].
//I: a string
//O: boolean
//C:
//E:

//                        Examples
//               Input	                       Output
// str: "(x + y) - (4)"           |   true
// str: "(x + y) - (4)"           |   true
// str: "(((10 ) ()) ((?)(:)))"   |   true
// str: "[{()}]"                  |   true
// str: "(50)("                   |   false
// str: "[{]}"                    |   false
const isBalanced = str => {
    // how to rep all balanced bracket pairs with a variable?
    //loop over str
    for (var i = 0; i < str.length; i++) {
        if (str[i] === )

    }
    //does index of string have ( and ) or { and } or [ and ]?
    // if yes return true
    //otherwise
    //return false
}        
